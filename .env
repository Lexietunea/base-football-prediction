# ============================================================
# BASE FOOTBALL PREDICTIONS — ENVIRONMENT VARIABLES
# ============================================================
# 1. Rename this file to .env
# 2. Fill in each value
# 3. NEVER commit .env to GitHub
# ============================================================

# ── DATABASE ────────────────────────────────────────────────
# Supabase: Settings → Database → Connection String → URI
# Railway:  Shown in your Postgres service dashboard
# Local:    postgresql://localhost:5432/basefootball
DATABASE_URL=postgresql://postgres.gbhbxslqxdklhrivnavd:Lexietunes123%@aws-1-eu-central-1.pooler.supabase.com:6543/postgres
# ── SERVER ──────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# ── ADMIN PROTECTION ────────────────────────────────────────
# Used to protect POST /results — only you can update scores
# Change this to something long and random!
ADMIN_KEY=change-this-to-a-long-random-secret-string